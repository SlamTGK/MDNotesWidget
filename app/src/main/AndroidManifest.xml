<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required to reschedule AlarmManager after device reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".MDNotesApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.MDNotesWidget">

        <!-- Main settings activity (also serves as widget config activity) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustResize">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <!-- Full-screen note viewer -->
        <activity
            android:name=".NoteViewerActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.MDNotesWidget.NoteViewer"
            android:taskAffinity=""
            android:excludeFromRecents="true" />

        <!-- History activity -->
        <activity
            android:name=".HistoryActivity"
            android:exported="false"
            android:label="@string/history"
            android:theme="@style/Theme.MDNotesWidget.NoteViewer"
            android:taskAffinity=""
            android:excludeFromRecents="true" />

        <!-- Favorites activity -->
        <activity
            android:name=".FavoritesActivity"
            android:exported="false"
            android:label="@string/favorites"
            android:theme="@style/Theme.MDNotesWidget.NoteViewer"
            android:taskAffinity=""
            android:excludeFromRecents="true" />

        <!-- Single note widget receiver -->
        <receiver
            android:name=".NoteWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="com.mdnotes.widget.ACTION_REFRESH" />
                <action android:name="com.mdnotes.widget.ACTION_OPEN_NOTE" />
                <action android:name="com.mdnotes.widget.ACTION_PERIODIC_UPDATE" />
                <action android:name="com.mdnotes.widget.ACTION_PIN_NOTE" />
                <action android:name="com.mdnotes.widget.ACTION_CREATE_NOTE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/note_widget_info" />
        </receiver>

        <!-- Scrollable single-note widget receiver -->
        <receiver
            android:name=".NoteListWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.mdnotes.widget.ACTION_SCROLL_REFRESH" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/note_list_widget_info" />
        </receiver>



    </application>
</manifest>
